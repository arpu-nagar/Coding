#include <bits/stdc++.h>
using namespace std;
using ll = long long int;
int main()
{
	int n;
	cin >> n;
	vector<pair<int,int>> arr(n);
	for(int i=0;i<n;i++){
		cin >> arr[i].first;
		arr[i].second = i+1;
	}
	sort(arr.begin(),arr.end());
	vector<int> a;
	for(int i=0;i<n;i++){
		a[i] = arr[i].first;
	}
	int q;
	cin >> q;
	while(q--){
		int m;
		cin >> m;

		auto x = lower_bound(a.begin(),a.end(),m);
		cout <<  << endl;
	}
	return 0;
}
