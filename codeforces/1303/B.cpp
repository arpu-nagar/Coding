#include <bits/stdc++.h>
using ll = long long int;

#define array arr;

using namespace std;

void solve()
{
    int n,g,b;
    cin >> n >> g >> b;
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}