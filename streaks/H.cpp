#include <bits/stdc++.h>
using ll = long long int;
using namespace std;
void solve()
{
    int n, m;
    cin >> n;
    vector<int> arr(n);
    for(auto &it : arr)
    cin >> it;

    cin >> m;
        vector<int> brr(n);

    for(auto &it : brr)
    cin >> it;

    for(int i=0;i<)
}

int main()
{
    // int t;
    // cin >> t;
    // while (t--)
        solve();

    return 0;
}
