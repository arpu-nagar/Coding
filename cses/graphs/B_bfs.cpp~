#include <bits/stdc++.h>
using namespace std;
using ll = long long int;
char g[1002][1002];
int n,m;

bool ok(int i,int j){
	if(i>=0 && j>=0 && i<n && j<m) return true;
	return false;
}

int main(){
    cin >> n >> m;

    for (int i = 0;i<n;i++)
        for (int j = 0; j < m;j++)
            cin >> g[i][j];
}

