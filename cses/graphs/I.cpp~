#include <bits/stdc++.h>
using namespace std;
using ll = long long int;
vector<array<int,3>> edge;
const ll inf = 1e18;
int main(){
	int n,e;
	cin >> n >> e;
	
	for(int i=0;i<e;i++){
		int a ,b,c;
		cin >> a >> b >> c;
		edge.push_back({a,b,c});
	}
	
}
